# Step 2: Load Dataset
df = pd.read_excel("Dry_Bean_Dataset.xlsx")

positive_group = {"SEKER", "BARBUNYA", "DERMASON"}

df["y_bin"] = df["Class"].isin(positive_group).astype(int)

print("Binary Class Distribution:\n", df["y_bin"].value_counts())
